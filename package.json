{
    "name": "menu-otp",
    "version": "0.1.0",
    "private": true,
    "description": "Website and release tooling for Menu OTP. The app itself is Swift; see app/Package.swift.",
    "_repository": "Where CHANGELOG.md's links point. Set explicitly so they name the real repository even when a clone is pushed somewhere else to try the pipeline out; without it the changelog follows whatever `origin` happens to be.",
    "repository": {
        "type": "git",
        "url": "https://github.com/iambriansreed/menu-otp.git"
    },
    "scripts": {
        "dev": "node .scripts/dev.mjs",
        "build": "cd web && skrapa build",
        "release": "node .scripts/release.mjs",
        "typecheck": "tsc -p .scripts"
    },
    "devDependencies": {
        "@types/node": "26.6.2",
        "commit-and-tag-version": "12.7.3",
        "skrapa": "0.8.0",
        "typescript": "^6.0.3"
    },
    "engines": {
        "node": ">=24"
    }
}
